package com.ams.enums;

public enum UserRole {
    ADMIN,   // Full access to everything
    MANAGER, // Manage assets, employees, departments, dashboard, backup
    USER     // View assets, request assignment, view own profile
}
